on:
  workflow_call:
    inputs:
      modulePath:
        required: true
        type: string

jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/templates/pwsh-publish/action.yaml
        with:
          modulePath: ${{ inputs.modulePath }}
