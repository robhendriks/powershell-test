on:
  workflow_call:
    inputs:
      modulePath:
        required: true
        type: string

jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
      - shell: pwsh
        run: |
          Write-Host '${{ inputs.modulePath }}'
