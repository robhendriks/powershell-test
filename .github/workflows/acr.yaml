name: Acr

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - src/Acr/**

permissions:
  contents: read
  packages: write

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  module:
    uses: ./.github/workflows/module-template.yaml
    with:
      modulePath: src/Acr
    secrets: inherit
